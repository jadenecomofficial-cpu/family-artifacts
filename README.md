# family-artifacts
Public static host (GitHub Pages) for recipient-facing artifacts — intakes,
study guides, static dashboards. Update = commit + push; recipient reloads.
Blank intakes carry no data. Personalized artifacts use unguessable filenames.
